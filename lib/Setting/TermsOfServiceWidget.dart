
import 'package:flutter/material.dart';

class TermsOfServiceWidget extends StatelessWidget {
  TermsOfServiceWidget({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('利用規約'),
      ),
    );
  }
}