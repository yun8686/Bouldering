
import 'package:flutter/material.dart';

class NoticeSettingWidget extends StatelessWidget {
  NoticeSettingWidget({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('通知設定'),
      ),
    );
  }
}